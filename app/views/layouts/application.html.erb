<!DOCTYPE html>
<html class="scroll-smooth">
  <head>
    <title>TickityTick</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%# only updates those DOM elements that have been modified %>
    <meta name="turbo-refresh-method" content="morph">
    <%# Preserves scroll position %>
    <meta name="turbo-refresh-scroll" content="preserve">

    <!-- Open Graph Tags - Displays rich preview of this site when link is shared -->
    <meta property="og:title" content="TickityTick" />
    <meta property="og:type" content="website" />
    <meta property="og:description" content="A distraction-free task manager designed to help you capture fleeting ideas without breaking your focus." />
    <meta property="og:url" content="<%= root_url %>" />
    <meta property="og:image" content="<%= image_url("og-image.png") %>" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="TickityTick" />
    <meta name="twitter:description" content="A distraction-free task manager designed to help you capture fleeting ideas without breaking your focus." />
    <meta name="twitter:url" content="<%= root_url %>" />
    <meta name="twitter:image" content="<%= image_url("og-image.png") %>" />

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <%# Favicon Ico %>
    <%= favicon_link_tag asset_path("favicon.ico") %>

    <%# Fonts %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <%# Titillium & Nunito Sans Fonts (All Weights) from https://fonts.google.com/ %>
    <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:ital,opsz,wght@0,6..12,200..1000;1,6..12,200..1000&family=Titillium+Web:ital,wght@0,200;0,300;0,400;0,600;0,700;0,900;1,200;1,300;1,400;1,600;1,700&display=swap" rel="stylesheet">
  </head>

  <body class="font-sans dark:text-gray-300 bg-primary-50 dark:bg-gray-900 selection:bg-secondary-200 selection:text-primary-900 dark:selection:bg-primary-200 dark:selection:text-primary-800">
    <!-- Top Navbar for Medium or higher screen size -->
    <%= render "shared/top_navbar" %>

    <%= render "shared/flash" %>

    <%= render "layouts/bg_polygon_top" %>

    <main class="grid gap-10 lg:gap-16 justify-items-center p-5">
      <%= yield %>
    </main>

    <%= render "layouts/bg_polygon_bottom" %>

    <%= render "layouts/footer" %>

    <!-- Bottom Navbar for Mobile Screen -->
    <%= render "shared/bottom_navbar" %>
  </body>
</html>