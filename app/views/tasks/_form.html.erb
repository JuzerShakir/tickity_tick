<div data-controller="character-counter" class="space-y-2 md:space-y-3">
  <%= form_with( model: task, class:"flex justify-center gap-2 sm:gap-4 md:gap-5") do |form| %>
    <%= form.text_field :description,
      placeholder: "Buy Groceries 🛒...",
      class: "form-input border-primary-700",
      data: { character_counter_target: "input"},
      maxlength: "100"
      %>
    <%= form.submit "Add", data: {turbo_submits_with: "Adding..."}, class: "form-submit w-auto" %>
  <% end %>

  <p class="text-xs font-thin text-primary-800 dark:text-primary-100 italic">
    <strong data-character-counter-target="counter">0</strong>&nbsp;/&nbsp;100
  </p>
</div>
